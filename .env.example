# Copy to `.env`. Anything already exported in your shell takes precedence over this file.

# Required only for live generation (`--live`). The demo, the compare view, and the whole
# test suite run without it, using the deterministic offline model.
ANTHROPIC_API_KEY=

# Model used for answer generation. Defaults to claude-opus-5.
DATAGRAPH_MODEL=claude-opus-5
